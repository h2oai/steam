import * as _ from 'lodash';
let initialState = {
  members: [],
  labels: []
};

export const collaboratorsReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
